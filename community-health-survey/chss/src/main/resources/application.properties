spring.application.name=community-health-survey

# --- Local MySQL connection (Week 3, Section 7) ---
# Create the database first: CREATE DATABASE health_survey_db;
spring.datasource.url=jdbc:mysql://localhost:3306/health_survey_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Jyotsna2005@

# --- JPA / Hibernate ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

server.port=8081
